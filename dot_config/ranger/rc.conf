# ===================================================================
# Custom Ranger Configuration - rc.conf
# Optimized for power user workflow on Arch Linux
# ===================================================================

# ===================================================================
# == Core Options
# ===================================================================

# Use miller columns (traditional ranger view)
set viewmode miller

# Column width ratios - slightly wider preview for better file inspection
set column_ratios 1,3,5

# SHOW HIDDEN FILES BY DEFAULT - Essential for dotfile management
set show_hidden true

# Hidden filter - Still hide certain cache/temp files even when showing hidden files
# Hides: Python cache, swap files, lost+found, __pycache__
set hidden_filter ^\.\.$|^\.git$|\.(?:pyc|pyo|bak|swp)$|^lost\+found$|^__(py)?cache__$

# Confirm deletion only for multiple files (not single files)
set confirm_on_delete multiple

# ===================================================================
# == Preview Settings
# ===================================================================

# Use custom preview script
set preview_script ~/.config/ranger/scope.sh
set use_preview_script true

# Enable previews for files and directories
set preview_files true
set preview_directories true
set collapse_preview true

# Wrap long lines in previews for better readability
set wrap_plaintext_previews true

# Enable image previews (if you use kitty terminal)
set preview_images true
set preview_images_method kitty

# ===================================================================
# == Performance & Behavior
# ===================================================================

# Automatically count files in directories (useful, slight performance cost)
set automatically_count_files true

# Auto-update cumulative directory sizes
set autoupdate_cumulative_size false

# Flush input after each keystroke (more responsive)
set flushinput true

# Save settings/history
set save_console_history true
set autosave_bookmarks true
set save_backtick_bookmark true

# ===================================================================
# == Display & UI
# ===================================================================

# Draw borders around columns - "separators" for clean column division
set draw_borders separators

# Show file sizes in both main column and status bar
set display_size_in_main_column true
set display_size_in_status_bar true

# Show free disk space in status bar
set display_free_space_in_status_bar true

# Display tags in all columns
set display_tags_in_all_columns true

# Status bar on bottom (traditional)
set status_bar_on_top false

# Draw progress bar for running tasks
set draw_progress_bar_in_status_bar true

# Use unicode ellipsis for cut-off filenames
set unicode_ellipsis true

# Show directory name in tabs
set dirname_in_tabs true

# ===================================================================
# == Title Bar Settings
# ===================================================================

# Update window title with current directory
set update_title true

# Update tmux/screen title
set update_tmux_title true

# Show hostname in title (useful for remote sessions)
set hostname_in_titlebar true

# Abbreviate home directory as ~ in titlebar
set tilde_in_titlebar true

# Shorten long titles (show last 3 directories)
set shorten_title 3

# ===================================================================
# == Version Control
# ===================================================================

# Enable VCS integration - show git status
set vcs_aware true
set vcs_backend_git enabled
set vcs_backend_hg disabled
set vcs_backend_bzr disabled
set vcs_backend_svn disabled

# Truncate long commit messages
set vcs_msg_length 50

# ===================================================================
# == File Operations
# ===================================================================

# Open all images in directory when opening one (for image viewers like feh)
set open_all_images true

# Enable mouse support
set mouse_enabled true

# Padding on right side when no preview
set padding_right true

# ===================================================================
# == History & Console
# ===================================================================

# History size for directory navigation and console commands
set max_history_size 50
set max_console_history_size 100

# Keep this many lines between cursor and top/bottom when scrolling
set scroll_offset 8

# ===================================================================
# == Sorting
# ===================================================================

# Default sort method
set sort natural
set sort_reverse false

# Sort directories first before files
set sort_directories_first true

# Case insensitive sorting
set sort_case_insensitive true

# ===================================================================
# == Colorscheme
# ===================================================================

# Default colorscheme (will be templated by chezmoi later for theming)
set colorscheme chrome_pulse_pink

# ===================================================================
# == Additional Features
# ===================================================================

# Show hidden files in bookmarks
set show_hidden_bookmarks true

# Use regexp in search by default

# Abbreviate $HOME with ~ in titles
set tilde_in_titlebar true

# Automatically show cumulative directory size
set autoupdate_cumulative_size false

# Idle delay for auto-updates (in milliseconds)
set idle_delay 2000

# Metadata delay for slow operations (in milliseconds)  
set metadata_deep_search false

# ===================================================================
# == Key Bindings
# ===================================================================

# Basic navigation
map Q quitall
map q quit
copymap q ZZ ZQ

map R     reload_cwd
map F     set freeze_files!
map <C-r> reset
map <C-l> redraw_window
map <C-c> abort
map <esc> change_mode normal
map ~ set viewmode!

# Navigation
map gg  move to=0
map ge  move to=-1
map gl  cd -r .
map gL  cd -r %f

# Jumping
map gh cd ~
map gd cd ~/Downloads
map gc cd ~/.config
map gv cd ~/.config/nvim
map gr cd /

# Tabs
map <C-n>     tab_new
map <C-w>     tab_close
map <TAB>     tab_move 1
map <S-TAB>   tab_move -1
map gt        tab_move 1
map gT        tab_move -1
map gn        tab_new
map gc        tab_close

# Quick tab access
map <a-1>     tab_open 1
map <a-2>     tab_open 2
map <a-3>     tab_open 3
map <a-4>     tab_open 4
map <a-5>     tab_open 5

# File operations
map yy copy
map dd cut
map pp paste
map po paste overwrite=True
map pP paste append=True
map pO paste overwrite=True append=True
map pl paste_symlink relative=False
map pL paste_symlink relative=True
map phl paste_hardlink
map pht paste_hardlinked_subtree
map dD console delete
map ud uncut
map da cut mode=add
map dr cut mode=remove
map dt cut mode=toggle

# Searching
map /  console search%space
map n  search_next
map N  search_next forward=False
map ct search_next order=tag
map cs search_next order=size
map ci search_next order=mimetype
map cc search_next order=ctime
map cm search_next order=mtime
map ca search_next order=atime

# Sorting
map or set sort_reverse!
map oz set sort=random
map os chain set sort=size;      set sort_reverse=False
map ob chain set sort=basename;  set sort_reverse=False
map on chain set sort=natural;   set sort_reverse=False
map om chain set sort=mtime;     set sort_reverse=False
map oc chain set sort=ctime;     set sort_reverse=False
map oa chain set sort=atime;     set sort_reverse=False
map ot chain set sort=type;      set sort_reverse=False
map oe chain set sort=extension; set sort_reverse=False

# Uppercase for reverse sort
map oS chain set sort=size;      set sort_reverse=True
map oB chain set sort=basename;  set sort_reverse=True
map oN chain set sort=natural;   set sort_reverse=True
map oM chain set sort=mtime;     set sort_reverse=True
map oC chain set sort=ctime;     set sort_reverse=True
map oA chain set sort=atime;     set sort_reverse=True
map oT chain set sort=type;      set sort_reverse=True
map oE chain set sort=extension; set sort_reverse=True

# Directory size calculation
map dc get_cumulative_size

# Toggle settings
map zc    set collapse_preview!
map zd    set sort_directories_first!
map zh    set show_hidden!
map <C-h> set show_hidden!
map zi    set preview_images!
map zm    set mouse_enabled!
map zp    set preview_files!
map zP    set preview_directories!
map zs    set sort_case_insensitive!
map zu    set autoupdate_cumulative_size!
map zv    set use_preview_script!
map zf    console filter%space

# Filter stack
map .d filter_stack add type d
map .f filter_stack add type f
map .l filter_stack add type l
map .m console filter_stack add mime%space
map .n console filter_stack add name%space
map .c filter_stack clear
map .. filter_stack show

# Bookmarks
map `<any>  enter_bookmark %any
map '<any>  enter_bookmark %any
map m<any>  set_bookmark %any
map um<any> unset_bookmark %any

# Chmod bindings
eval for arg in "rwxXst": cmd("map +u{0} shell -f chmod u+{0} %s".format(arg))
eval for arg in "rwxXst": cmd("map +g{0} shell -f chmod g+{0} %s".format(arg))
eval for arg in "rwxXst": cmd("map +o{0} shell -f chmod o+{0} %s".format(arg))
eval for arg in "rwxXst": cmd("map +a{0} shell -f chmod a+{0} %s".format(arg))
eval for arg in "rwxXst": cmd("map +{0}  shell -f chmod u+{0} %s".format(arg))

eval for arg in "rwxXst": cmd("map -u{0} shell -f chmod u-{0} %s".format(arg))
eval for arg in "rwxXst": cmd("map -g{0} shell -f chmod g-{0} %s".format(arg))
eval for arg in "rwxXst": cmd("map -o{0} shell -f chmod o-{0} %s".format(arg))
eval for arg in "rwxXst": cmd("map -a{0} shell -f chmod a-{0} %s".format(arg))
eval for arg in "rwxXst": cmd("map -{0}  shell -f chmod u-{0} %s".format(arg))

# ===================================================================
# == Console Keybindings
# ===================================================================

# Basic
cmap <tab>   eval fm.ui.console.tab()
cmap <s-tab> eval fm.ui.console.tab(-1)
cmap <ESC>   eval fm.ui.console.close()
cmap <CR>    eval fm.ui.console.execute()
cmap <C-l>   redraw_window

copycmap <ESC> <C-c>
copycmap <CR>  <C-j>

# Movement
cmap <up>    eval fm.ui.console.history_move(-1)
cmap <down>  eval fm.ui.console.history_move(1)
cmap <left>  eval fm.ui.console.move(left=1)
cmap <right> eval fm.ui.console.move(right=1)
cmap <home>  eval fm.ui.console.move(right=0, absolute=True)
cmap <end>   eval fm.ui.console.move(right=-1, absolute=True)
cmap <a-b>   eval fm.ui.console.move_word(left=1)
cmap <a-f>   eval fm.ui.console.move_word(right=1)

# Line editing
cmap <backspace>  eval fm.ui.console.delete(-1)
cmap <delete>     eval fm.ui.console.delete(0)
cmap <C-w>        eval fm.ui.console.delete_word()
cmap <A-d>        eval fm.ui.console.delete_word(backward=False)
cmap <C-k>        eval fm.ui.console.delete_rest(1)
cmap <C-u>        eval fm.ui.console.delete_rest(-1)
cmap <C-y>        eval fm.ui.console.paste()

# Emacs-style bindings
copycmap <ESC>       <C-g>
copycmap <up>        <C-p>
copycmap <down>      <C-n>
copycmap <left>      <C-b>
copycmap <right>     <C-f>
copycmap <home>      <C-a>
copycmap <end>       <C-e>
copycmap <delete>    <C-d>
copycmap <backspace> <C-h>

# ===================================================================
# == Pager Keybindings
# ===================================================================

pmap  <down>      pager_move  down=1
pmap  <up>        pager_move  up=1
pmap  <left>      pager_move  left=4
pmap  <right>     pager_move  right=4
pmap  <home>      pager_move  to=0
pmap  <end>       pager_move  to=-1
pmap  <pagedown>  pager_move  down=1.0  pages=True
pmap  <pageup>    pager_move  up=1.0    pages=True
pmap  <C-d>       pager_move  down=0.5  pages=True
pmap  <C-u>       pager_move  up=0.5    pages=True

copypmap <UP>       k  <C-p>
copypmap <DOWN>     j  <C-n> <CR>
copypmap <LEFT>     h
copypmap <RIGHT>    l
copypmap <HOME>     g
copypmap <END>      G
copypmap <C-d>      d
copypmap <C-u>      u
copypmap <PAGEDOWN> n  f  <C-F>  <Space>
copypmap <PAGEUP>   p  b  <C-B>

pmap     <C-l> redraw_window
pmap     <ESC> pager_close
copypmap <ESC> q Q i <F3>
pmap E      edit_file

# ===================================================================
# == Taskview Keybindings
# ===================================================================

tmap <up>        taskview_move up=1
tmap <down>      taskview_move down=1
tmap <home>      taskview_move to=0
tmap <end>       taskview_move to=-1
tmap <pagedown>  taskview_move down=1.0  pages=True
tmap <pageup>    taskview_move up=1.0    pages=True
tmap <C-d>       taskview_move down=0.5  pages=True
tmap <C-u>       taskview_move up=0.5    pages=True

copytmap <UP>       k  <C-p>
copytmap <DOWN>     j  <C-n> <CR>
copytmap <HOME>     g
copytmap <END>      G
copytmap <C-u>      u
copytmap <PAGEDOWN> n  f  <C-F>  <Space>
copytmap <PAGEUP>   p  b  <C-B>

tmap J          eval -q fm.ui.taskview.task_move(-1)
tmap K          eval -q fm.ui.taskview.task_move(0)
tmap dd         eval -q fm.ui.taskview.task_remove()
tmap <delete>   eval -q fm.ui.taskview.task_remove()

tmap <C-l> redraw_window
tmap <ESC> taskview_close
copytmap <ESC> q Q w <C-c>
